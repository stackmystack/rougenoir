# Contributing

# Rules

1. Miri should always pass.
1. All doc tests should be replicated as unit tests.
   [`nextest`](https://nexte.st/) doesn't cover them, and it's the default.
1. Every commit should pass the CI.

# TODO
